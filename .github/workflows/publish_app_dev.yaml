name: app-build-android

on:
  workflow_dispatch:
  push:
    tags:
      - "dev_v*"

jobs:
  build-android:
    runs-on: ubuntu-latest  # 改成 ubuntu 更快；如果必须 macos 就改回 macos-latest
    permissions:
      contents: write
    steps:
      # 签出代码（改成 master，你的仓库默认是 master 分支）
      - uses: actions/checkout@v4
        with:
          ref: master

      # 设置 JAVA 环境
      - uses: actions/setup-java@v4
        with:
          distribution: "zulu"
          java-version: "17"
          cache: "gradle"

      # 设置 Flutter（3.38.x 匹配项目 README）
      - name: Flutter action
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.38.x"
          cache: true

      # 诊断环境（强烈建议保留，帮你看问题）
      - name: Flutter Doctor
        run: flutter doctor -v

      # 清理旧构建（防止缓存问题导致 silent fail）
      - name: Flutter Clean
        run: |
          cd simple_live_app
          flutter clean

      # 更新 packages
      - name: Restore packages
        run: |
          cd simple_live_app
          flutter pub get

      # 安装 flutter_distributor（原代码有，保留）
      - name: Install flutter_distributor
        run: dart pub global activate flutter_distributor

      # 打包 APK（无签名 + verbose 日志）
      - name: Build APK
        run: |
          cd simple_live_app
          flutter build apk --release --split-per-abi --verbose

      # 上传 Artifacts
      - name: Upload APK to Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: android
          path: |
            simple_live_app/build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk
            simple_live_app/build/app/outputs/flutter-apk/app-arm64-v8a-release.apk
            simple_live_app/build/app/outputs/flutter-apk/app-x86_64-release.apk

      # 完成
      - run: echo "🍏 This job's status is ${{ job.status }}."
